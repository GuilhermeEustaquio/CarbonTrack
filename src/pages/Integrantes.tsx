import { Card } from '../components/ui';
const integrantes = [{ nome: 'Guilherme Eustaquio', rm: '566784', turma: '1TDSPS' }, { nome: 'Caio Cantini Couto', rm: '563452', turma: '1TDSPS' }, { nome: 'Matheus Tavares', rm: '566844', turma: '1TDSPS' }];
export function Integrantes() { return <div className="view-enter"><div className="page-head"><div><div className="eyebrow">Equipe</div><h1>Integrantes</h1><div className="sub">Dados fáceis de editar. Links ausentes permanecem como placeholders para não inventar informação.</div></div></div><div className="grid-2 info-grid">{integrantes.map(i => <Card key={i.rm} title={i.nome} icon="user" sub={`RM ${i.rm} · ${i.turma}`}><div className="cell-co" style={{ alignItems: 'flex-start' }}><div className="logo" style={{ width: 64, height: 64, flexBasis: 64 }}>{i.nome.split(' ').map(n => n[0]).slice(0, 2).join('')}</div><div><p className="t-muted" style={{ margin: '0 0 6px' }}>Foto: [INSERIR FOTO]</p><p className="t-muted" style={{ margin: '0 0 6px' }}>GitHub: [INSERIR LINK DO GITHUB]</p><p className="t-muted" style={{ margin: 0 }}>LinkedIn: [INSERIR LINKEDIN]</p></div></div></Card>)}</div></div>; }
